# Changelog

## 0.1.0

- Unified navigation, action and control geometry in one inline SVG renderer; removed the application's mask/sprite paths and icon tiles.
- Added the initial semantic icon registry.
