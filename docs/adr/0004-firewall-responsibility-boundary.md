# 安装器生成网络放行清单但不修改防火墙

裸机安装器负责根据 Web、Node 或 combined 拓扑生成最小网络放行清单、检查明显暴露风险并在验证结果中明确提示，但不自动修改 nftables、ufw 或云安全组。操作者负责按清单实施来源限制，真实 VPS 验收必须验证该边界。这个选择保留了对云厂商与既有 SSH 策略的兼容性，避免安装器导致远程失联；代价是安装成功不能被解释为主机和云侧网络隔离已经自动完成。
